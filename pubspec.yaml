name: agenda_contatos
description: AgendaContatos - Denner DEV
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^2.0.0
  sqflite: ^1.1.5
  url_launcher: ^5.0.2
  image_picker: ^0.6.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/images/

  uses-material-design: true
